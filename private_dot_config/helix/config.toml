theme = "catppuccin_macchiato"

[editor]
color-modes = true
cursorline = true
rulers = [80, 100, 120]
trim-trailing-whitespace = true
insert-final-newline = true
trim-final-newlines = false
true-color = true

preview-completion-insert = false

[editor.auto-save]
after-delay.enable = true
focus-lost = true

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[keys.normal]
"[" = { "[" = "select_prev_sibling" }
"]" = { "]" = "select_next_sibling" }

S-up = "select_line_above"
S-down = "select_line_below"

[keys.normal.space.space]
# Custom shortcuts.

w = ":w"
f = ":format"
R = [":reload-all", ":config-reload", ":lsp-restart"]

"/" = ":sh zellij run --floating --width 90%% --height 90%% -c -- scooter > /dev/null" # Run scooter (find and replace).
L = ":sh zellij run --floating -c -- lf $(dirname %{buffer_name}) > /dev/null" # Open lf in the dir of current buffer.
l = ":sh zellij run --floating -c -- lf > /dev/null" # Open lf.

T = ":sh zellij run --floating -c --cwd $(dirname %{buffer_name}) -- fish > /dev/null"
t = ":sh zellij run --floating -c -- fish > /dev/null"

k = "remove_selections" # Default binding conflicts with zellij.

[keys.select.space.space]
k = "remove_selections" # Default binding conflicts with zellij.


[keys.select.space.space.r]
# Rendering.

w = ":toggle whitespace.render all none" # On/Off whitespace rendering.
z = ":toggle soft-wrap.enable"           # On/Off soft-wrap.

[keys.normal.space.space.r]
# Rendering.

w = ":toggle whitespace.render all none" # On/Off whitespace rendering.
z = ":toggle soft-wrap.enable"           # On/Off soft-wrap.

[keys.normal.space.space.g]
# Git helpers

g = ":sh zellij run --floating --width 90%% --height 90%% -c -- gitu > /dev/null"
B = ":sh zellij run --floating --width 90%% --height 90%% -c -- gitu blame %{buffer_name} > /dev/null"
b = ":echo %sh{git blame -L %{cursor_line},+1 %{buffer_name}}"
d = ":sh zellij run --floating --width 90%% --height 90%% -c -- fish -c 'git --paginate diff -- %{buffer_name} | delta --paging=always' > /dev/null"
h = ":sh zellij run --floating --width 90%% --height 90%% -c -- fish -c 'git-hunk-at-line %{buffer_name} %{cursor_line} | delta --paging=always' > /dev/null"
K = ":reset-diff-change"


[keys.normal.space.q]
# Close.

q = ":quit-all"
Q = ":quit-all!"
x = ":write-quit"

